{
  "name": "yatt-mcp",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Servidor MCP de YATT: la IA gestiona tests, corre corridas headless y controla un navegador en vivo (100% local).",
  "scripts": {
    "dev": "bun run src/server.ts",
    "test": "bun run test/smoke.ts",
    "test:manual": "bun run test/manual.ts",
    "typecheck": "bunx tsc -p tsconfig.json"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0"
  }
}